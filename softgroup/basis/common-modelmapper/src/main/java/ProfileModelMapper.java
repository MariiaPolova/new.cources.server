import org.modelmapper.ModelMapper;

/**
 * Created by polev on 04.03.2017.
 */
public class ProfileModelMapper{
    static void mapAutomatically() {
 /*      parameters (Profile objToMap)
        Profile profile = objToMap.createProfile();
        ModelMapper modelMapper = new ModelMapper();
        ProfileDTO profileDTO = modelMapper.map(order, ProfileDTODTO.class);
        assertProfilesEqual(profile, profileDTO);
        return profile;
   */ }
}
